diff --git a/crypto/CMakeLists.txt b/crypto/CMakeLists.txt
index 7066cc8..ad70dfc 100644
--- a/crypto/CMakeLists.txt
+++ b/crypto/CMakeLists.txt
@@ -989,9 +989,15 @@ target_link_libraries(crypto ${PLATFORM_LIBS})
 if (WIN32)
 	set(CRYPTO_POSTFIX -${CRYPTO_MAJOR_VERSION})
 endif()
+
 set_target_properties(crypto PROPERTIES
 	OUTPUT_NAME crypto${CRYPTO_POSTFIX}
-	ARCHIVE_OUTPUT_NAME crypto${CRYPTO_POSTFIX})
+	ARCHIVE_OUTPUT crypto${CRYPTO_POSTFIX})
+
+set_target_properties(crypto PROPERTIES
+	OUTPUT_NAME_DEBUG cryptod${CRYPTO_POSTFIX}
+	ARCHIVE_OUTPUT_NAME_DEBUG cryptod${CRYPTO_POSTFIX})
+
 set_target_properties(crypto PROPERTIES VERSION
 	${CRYPTO_VERSION} SOVERSION ${CRYPTO_MAJOR_VERSION})
 
