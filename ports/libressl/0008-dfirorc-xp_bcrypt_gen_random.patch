diff --git a/crypto/CMakeLists.txt b/crypto/CMakeLists.txt
index 75e398a..8e8c679 100644
--- a/crypto/CMakeLists.txt
+++ b/crypto/CMakeLists.txt
@@ -804,6 +804,7 @@ endif()
 if(WIN32)
 	set(CRYPTO_SRC ${CRYPTO_SRC} compat/posix_win.c)
 	set(CRYPTO_SRC ${CRYPTO_SRC} compat/inet_pton.c)
+	set(CRYPTO_SRC ${CRYPTO_SRC} compat/bcrypt_gen_random.c)
 	set(EXTRA_EXPORT ${EXTRA_EXPORT} gettimeofday)
 	set(EXTRA_EXPORT ${EXTRA_EXPORT} getuid)
 	set(EXTRA_EXPORT ${EXTRA_EXPORT} posix_perror)
diff --git a/crypto/compat/getentropy_win.c b/crypto/compat/getentropy_win.c
index 64514b3..8ee21ca 100644
--- a/crypto/compat/getentropy_win.c
+++ b/crypto/compat/getentropy_win.c
@@ -41,7 +41,7 @@ getentropy(void *buf, size_t len)
 		return (-1);
 	}
 
-	if (FAILED(BCryptGenRandom(NULL, buf, len, BCRYPT_USE_SYSTEM_PREFERRED_RNG))) {
+	if (FAILED(BCryptGenRandomWrapper(NULL, buf, len, BCRYPT_USE_SYSTEM_PREFERRED_RNG))) {
 		errno = EIO;
 		return (-1);
 	}
